#include <stdio.h>

int main()
{int size,count=0,count1=0;
scanf("%d",&size);
int a[size];
for(int i=0;i<size;i++)
{
    scanf("%d",&a[i]);
}
for(int i=0;i<size;i++)
{
    if(a[i]==0)
    {count++;}
    else
    {count1++;}
}
printf("zeors=%d\nones=%d",count,count1);
return 0;
}
